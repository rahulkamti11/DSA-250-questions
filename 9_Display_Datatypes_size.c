#include <stdio.h>

int main() {
    int i;float f;double d;char c;
    printf("The size of integer datatype is %d bytes",sizeof(i));
    printf("\nThe size of float datatype is %d bytes",sizeof(f));
    printf("\nThe size of double datatype is %d bytes",sizeof(d));
    printf("\nThe size of character datatype is %d bytes",sizeof(c));
   
    return 0;
}